# Environment configuration for Proxmox deployment
# This file is used when building for the teacher's Proxmox server

# Base URL path where the site will be served
# Change this to match your Proxmox web server subdirectory
VITE_BASE_PATH=/kxrim/

# Proxmox server details (for documentation only)
# SERVER_IP=10.9.14.x
# DEPLOYMENT_PATH=/var/www/html/kxrim/
